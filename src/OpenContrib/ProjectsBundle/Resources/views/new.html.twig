{% extends 'base.html.twig' %}

{% block body %}
    <h1>Créer un projet</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Valider" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('projects_index') }}">Retour à la liste des projets</a>
        </li>
    </ul>
{% endblock %}
